# =============================================================================
# Logstash Consumer Deployment
# =============================================================================
# Reads from 'logs' topic via consumer group, writes to 'logs-processed'
# =============================================================================
apiVersion: apps/v1
kind: Deployment
metadata:
  name: logstash-consumer
  namespace: korvet
  labels:
    app: logstash-consumer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logstash-consumer
  template:
    metadata:
      labels:
        app: logstash-consumer
    spec:
      nodeSelector:
        node-type: workloads
      containers:
        - name: logstash
          image: docker.elastic.co/logstash/logstash:8.12.0
          args:
            - "-f"
            - "/usr/share/logstash/pipeline/logstash.conf"
          resources:
            requests:
              memory: "1Gi"
              cpu: "500m"
            limits:
              memory: "2Gi"
              cpu: "1000m"
          ports:
            - containerPort: 9601
              name: http
          volumeMounts:
            - name: pipeline
              mountPath: /usr/share/logstash/pipeline
          env:
            - name: XPACK_MONITORING_ENABLED
              value: "false"
            - name: LOG_LEVEL
              value: "info"
            - name: PIPELINE_WORKERS
              value: "4"
            - name: PIPELINE_BATCH_SIZE
              value: "500"
      volumes:
        - name: pipeline
          configMap:
            name: logstash-consumer-pipeline
            items:
              - key: logstash.conf
                path: logstash.conf
